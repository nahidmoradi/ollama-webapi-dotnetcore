# 📋 مستندات کامل پروژه Comment Analyzer

## 🎯 هدف پروژه

این پروژه یک سیستم **تحلیل هوشمند کامنت‌ها** است که مشابه سیستم خلاصه‌سازی کامنت‌های دیجی‌کالا عمل می‌کند. با استفاده از هوش مصنوعی محلی (بدون نیاز به API خارجی)، کامنت‌های فارسی کاربران را تحلیل و خلاصه می‌کند.

### ویژگی‌های اصلی:
- ✅ **تحلیل احساسات**: تشخیص مثبت/منفی/خنثی بودن کامنت‌ها
- ✅ **استخراج نکات کلیدی**: جدا کردن نکات مثبت و منفی
- ✅ **موضوع‌بندی**: شناسایی تم‌های اصلی (کیفیت، قیمت، ارسال و...)
- ✅ **خلاصه‌سازی**: تولید خلاصه 3 خطی از دیدگاه کلی کاربران
- ✅ **پردازش فارسی**: کاملاً فارسی‌زبان و بهینه شده برای متن فارسی

---

## 🏗️ معماری پروژه

پروژه از معماری **Full-Stack** تشکیل شده:

```
┌─────────────────────────────────────────────────────┐
│                  Frontend (Angular 18)              │
│  ┌─────────────────────────────────────────────┐   │
│  │  Comment Analyzer Component                 │   │
│  │  - مدیریت کامنت‌ها                         │   │
│  │  - نمایش نتایج تحلیل                       │   │
│  │  - رابط کاربری RTL                         │   │
│  └──────────────┬──────────────────────────────┘   │
│                 │ HTTP Requests                     │
│                 ▼                                    │
│  ┌─────────────────────────────────────────────┐   │
│  │  Comment Analyzer Service                   │   │
│  │  - ارتباط با Backend API                   │   │
│  └──────────────┬──────────────────────────────┘   │
└─────────────────┼──────────────────────────────────┘
                  │
                  │ REST API (Port 4201 → 5100)
                  │
┌─────────────────▼──────────────────────────────────┐
│           Backend (.NET 9 + ASP.NET Core)          │
│  ┌─────────────────────────────────────────────┐   │
│  │  CommentController (REST API)               │   │
│  │  - /api/comment/analyze                     │   │
│  │  - /api/comment/summary                     │   │
│  │  - /api/comment/detailed                    │   │
│  └──────────────┬──────────────────────────────┘   │
│                 │                                    │
│                 ▼                                    │
│  ┌─────────────────────────────────────────────┐   │
│  │  CommentAnalyzerService                     │   │
│  │  - ساخت Prompt ساختاریافته                │   │
│  │  - پردازش پاسخ Ollama                      │   │
│  │  - مدیریت استریم                           │   │
│  └──────────────┬──────────────────────────────┘   │
│                 │                                    │
│                 ▼                                    │
│  ┌─────────────────────────────────────────────┐   │
│  │  OllamaSharp Client                         │   │
│  │  - ارتباط با Ollama API                    │   │
│  │  - مدیریت Chat Completion                  │   │
│  └──────────────┬──────────────────────────────┘   │
└─────────────────┼──────────────────────────────────┘
                  │
                  │ HTTP (Port 11434)
                  │
┌─────────────────▼──────────────────────────────────┐
│              Ollama (Local AI Server)              │
│  ┌─────────────────────────────────────────────┐   │
│  │  Llama 3.1:latest Model (4.9 GB)           │   │
│  │  - پردازش زبان طبیعی فارسی                │   │
│  │  - تولید پاسخ‌های ساختاریافته             │   │
│  │  - اجرا روی CPU/GPU محلی                   │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

---

## 📚 کتابخانه‌ها و تکنولوژی‌های استفاده شده

### Backend (.NET)

#### 1. **OllamaSharp 3.0.8** 
📦 کتابخانه اصلی برای ارتباط با Ollama

```csharp
// نصب
dotnet add package OllamaSharp

// استفاده
var client = new OllamaApiClient("http://localhost:11434");
client.SelectedModel = "llama3.1:latest";
await foreach (var response in client.Chat(chatRequest))
{
    // دریافت استریم پاسخ
}
```

**ویژگی‌ها:**
- ✅ پشتیبانی از Chat Completion
- ✅ استریمینگ پاسخ (Streaming)
- ✅ مدیریت Context و History
- ✅ پشتیبانی کامل از API Ollama

#### 2. **Microsoft.SemanticKernel 1.31.0**
🧠 فریم‌ورک هوش مصنوعی مایکروسافت

```csharp
// استفاده برای Orchestration و Plugin Management
builder.Services.AddKernel()
    .AddOllamaTextGeneration("llama3.1:latest", "http://localhost:11434");
```

**ویژگی‌ها:**
- ✅ مدیریت Prompt Templates
- ✅ Chain of Thought پیشرفته
- ✅ Plugin System برای توسعه

#### 3. **ASP.NET Core 9.0**
🌐 فریم‌ورک وب برای ساخت REST API

```csharp
// تنظیمات CORS برای دسترسی Angular
builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowAll", builder =>
    {
        builder.AllowAnyOrigin()
               .AllowAnyMethod()
               .AllowAnyHeader();
    });
});
```

#### 4. **Swashbuckle.AspNetCore 9.0.6**
📖 ابزار تولید Swagger UI برای مستندات API

```csharp
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();
```

### Frontend (Angular)

#### 1. **Angular 18 (Standalone Components)**
⚡ فریم‌ورک SPA با معماری مدرن

```typescript
// استفاده از Standalone Component
@Component({
  selector: 'app-comment-analyzer',
  standalone: true,
  imports: [CommonModule, FormsModule, HttpClientModule],
  templateUrl: './comment-analyzer.component.html'
})
export class CommentAnalyzerComponent { }
```

**ویژگی‌های استفاده شده:**
- ✅ Standalone Components (بدون NgModule)
- ✅ Signals API برای Reactivity
- ✅ HttpClient برای API Calls
- ✅ FormsModule برای Two-Way Binding

#### 2. **RxJS**
🔄 کتابخانه Reactive Programming

```typescript
this.commentService.analyzeComments(this.comments).subscribe({
  next: (result) => {
    this.summary = result;
    this.isLoading = false;
  },
  error: (error) => {
    console.error('خطا در تحلیل:', error);
  }
});
```

#### 3. **SCSS**
🎨 استایل‌دهی پیشرفته با متغیرها و Nesting

```scss
.analyzer-container {
  direction: rtl;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  
  .comment-card {
    border-radius: 12px;
    box-shadow: 0 8px 16px rgba(0,0,0,0.1);
  }
}
```

---

## 🔌 نحوه اتصال به Ollama و استفاده از Llama 3.1

### مرحله 1: تنظیمات Backend

#### فایل `appsettings.json`:
```json
{
  "Ollama": {
    "Endpoint": "http://localhost:11434",
    "ModelName": "llama3.1:latest"
  }
}
```

#### فایل `Program.cs`:
```csharp
// خواندن تنظیمات از appsettings.json
var ollamaEndpoint = builder.Configuration["Ollama:Endpoint"] 
                     ?? "http://localhost:11434";
var modelName = builder.Configuration["Ollama:ModelName"] 
                     ?? "llama3.1:latest";

// ثبت OllamaApiClient به عنوان Singleton
builder.Services.AddSingleton<IOllamaApiClient>(sp =>
{
    var client = new OllamaApiClient(ollamaEndpoint);
    client.SelectedModel = modelName;
    return client;
});

// ثبت سرویس تحلیل کامنت
builder.Services.AddScoped<ICommentAnalyzerService, CommentAnalyzerService>();
```

### مرحله 2: استفاده در Service

#### فایل `CommentAnalyzerService.cs`:

```csharp
public class CommentAnalyzerService : ICommentAnalyzerService
{
    private readonly IOllamaApiClient _ollamaClient;

    public CommentAnalyzerService(IOllamaApiClient ollamaClient)
    {
        _ollamaClient = ollamaClient;
    }

    public async Task<CommentSummary> AnalyzeCommentsAsync(List<Comment> comments)
    {
        // 1️⃣ آماده‌سازی Prompt
        var prompt = BuildAnalysisPrompt(comments);

        // 2️⃣ ساخت ChatRequest
        var chatRequest = new ChatRequest
        {
            Model = "llama3.1:latest",  // ✅ مشخص کردن مدل
            Messages = new List<Message>
            {
                new Message
                {
                    Role = "system",
                    Content = "تو یک تحلیلگر حرفه‌ای نظرات کاربران هستی..."
                },
                new Message
                {
                    Role = "user",
                    Content = prompt
                }
            },
            Stream = true  // ✅ فعال‌سازی استریمینگ
        };

        // 3️⃣ ارسال به Ollama و دریافت پاسخ
        var result = new StringBuilder();
        await foreach (var response in _ollamaClient.Chat(chatRequest))
        {
            result.Append(response.Message?.Content ?? "");
        }

        // 4️⃣ پردازش پاسخ JSON
        return ParseAnalysisResult(result.ToString());
    }
}
```

### مرحله 3: ساختار Prompt برای Llama 3.1

```csharp
private string BuildAnalysisPrompt(List<Comment> comments)
{
    return $@"
تو یک تحلیلگر حرفه‌ای نظرات کاربران هستی. کامنت‌های زیر را تحلیل کن:

📝 کامنت‌ها:
{string.Join("\n", comments.Select((c, i) => $"{i + 1}. {c.Text}"))}

🎯 خروجی مورد انتظار (فقط JSON):
{{
  ""positivePoints"": [""نکته مثبت 1"", ""نکته مثبت 2""],
  ""negativePoints"": [""نکته منفی 1"", ""نکته منفی 2""],
  ""commonThemes"": [""کیفیت"", ""قیمت"", ""ارسال""],
  ""overallSentiment"": ""positive"",
  ""confidence"": 85,
  ""summary"": ""خلاصه سه خطی...""
}}

⚠️ فقط JSON خروجی بده، بدون توضیحات اضافی!
";
}
```

### مرحله 4: فلوچارت فرآیند

```
┌─────────────────────────┐
│  کاربر کامنت‌ها را     │
│  وارد می‌کند           │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Angular Frontend       │
│  ارسال POST Request     │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  ASP.NET Backend        │
│  CommentController      │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  CommentAnalyzerService │
│  - ساخت Prompt         │
│  - آماده‌سازی Request  │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  OllamaSharp Client     │
│  ارسال ChatRequest      │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Ollama Server          │
│  Port: 11434            │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Llama 3.1:latest       │
│  پردازش متن فارسی      │
│  تولید JSON            │
└───────────┬─────────────┘
            │
            ▼ (Streaming)
┌─────────────────────────┐
│  OllamaSharp            │
│  دریافت استریم پاسخ    │
│  await foreach          │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  CommentAnalyzerService │
│  پارس کردن JSON        │
│  ساخت CommentSummary    │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  CommentController      │
│  بازگشت Response       │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Angular Frontend       │
│  نمایش نتایج           │
└─────────────────────────┘
```

---

## 🎯 مزایای استفاده از Llama 3.1 محلی

### 1. **حریم خصوصی کامل**
- ✅ تمام داده‌ها روی سرور محلی پردازش می‌شوند
- ✅ هیچ اطلاعاتی به API خارجی ارسال نمی‌شود
- ✅ مناسب برای داده‌های حساس و محرمانه

### 2. **بدون هزینه**
- ✅ نیازی به پرداخت برای API های ChatGPT یا Claude نیست
- ✅ بدون محدودیت تعداد درخواست
- ✅ یکبار نصب، همیشه رایگان

### 3. **سرعت بالا**
- ✅ بدون تأخیر شبکه برای ارسال به سرور خارجی
- ✅ پاسخ‌های سریع‌تر (بسته به سخت‌افزار)
- ✅ استریمینگ واقعی بدون buffering

### 4. **قابلیت تنظیم**
- ✅ کنترل کامل روی Temperature و Top-P
- ✅ امکان Fine-Tune کردن مدل
- ✅ تنظیم Context Length

### 5. **پشتیبانی عالی از فارسی**
- ✅ Llama 3.1 روی متن‌های چندزبانه آموزش دیده
- ✅ درک دستور فارسی و تولید خروجی فارسی
- ✅ پردازش صحیح RTL

---

## 📊 مقایسه Llama 3.1 با سایر مدل‌ها

| ویژگی | Llama 3.1 (محلی) | GPT-4 API | Claude API |
|-------|------------------|-----------|------------|
| **هزینه** | رایگان | $0.03/1K token | $0.015/1K token |
| **حریم خصوصی** | ✅ کامل | ❌ ابری | ❌ ابری |
| **سرعت** | ⚡ سریع (محلی) | 🌐 شبکه | 🌐 شبکه |
| **فارسی** | ✅ خوب | ✅ عالی | ✅ عالی |
| **Offline** | ✅ بله | ❌ خیر | ❌ خیر |
| **Context** | 8K-128K tokens | 128K tokens | 200K tokens |
| **نیاز سخت‌افزاری** | 8GB+ RAM | - | - |

---

## 🚀 نکات بهینه‌سازی

### 1. **Context Management**
```csharp
var chatRequest = new ChatRequest
{
    Model = "llama3.1:latest",
    Messages = messages,
    Options = new ModelOptions
    {
        Temperature = 0.7f,      // کنترل خلاقیت
        TopP = 0.9f,             // Nucleus Sampling
        NumCtx = 4096,           // تعداد توکن Context
        RepeatPenalty = 1.1f     // جلوگیری از تکرار
    }
};
```

### 2. **Prompt Engineering**
```csharp
// ✅ خوب: واضح و ساختاریافته
var prompt = @"
تحلیل کن و JSON خروجی بده:
{""sentiment"": ""positive""}
";

// ❌ بد: مبهم
var prompt = "این کامنت‌ها رو ببین";
```

### 3. **استریمینگ برای UX بهتر**
```csharp
await foreach (var response in _ollamaClient.Chat(chatRequest))
{
    // ارسال به فرانت به صورت Real-time
    await Clients.Caller.SendAsync("ReceiveChunk", 
                                   response.Message?.Content);
}
```

---

## 🔧 عیب‌یابی رایج

### ❌ خطا: "Model not found"
```bash
# راه‌حل: دانلود مدل
ollama pull llama3.1:latest
```

### ❌ خطا: "Connection refused"
```bash
# راه‌حل: راه‌اندازی Ollama
ollama serve
```

### ❌ خطا: "Out of memory"
```bash
# راه‌حل: استفاده از مدل کوچکتر
ollama pull llama3.1:8b  # نسخه کوچکتر
```

### ❌ خطا: "CORS Policy"
```csharp
// راه‌حل: اضافه کردن CORS در Program.cs
app.UseCors("AllowAll");
```

---

## 📈 آمار پروژه

- **تعداد خطوط کد Backend**: ~500 خط
- **تعداد خطوط کد Frontend**: ~400 خط
- **تعداد Endpoints**: 5 API
- **زمان پاسخ میانگین**: 2-5 ثانیه (بسته به تعداد کامنت)
- **حداکثر کامنت قابل پردازش**: 100+ (محدود به Context Length)

---

## 🎓 منابع یادگیری

### OllamaSharp
- 📦 [مستندات OllamaSharp](https://github.com/awaescher/OllamaSharp)
- 📚 [نمونه‌های کد](https://github.com/awaescher/OllamaSharp/tree/main/samples)

### Ollama
- 🌐 [وبسایت رسمی](https://ollama.ai/)
- 📖 [API Documentation](https://github.com/ollama/ollama/blob/main/docs/api.md)
- 🤖 [Model Library](https://ollama.ai/library)

### Llama 3.1
- 📄 [Model Card](https://ollama.ai/library/llama3.1)
- 🔬 [تحقیقات Meta AI](https://ai.meta.com/llama/)

---

## 🤝 مشارکت و توسعه

### ایده‌های توسعه آینده:
- [ ] اضافه کردن نمودار تحلیل احساسات
- [ ] پشتیبانی از چند زبان
- [ ] ذخیره تاریخچه تحلیل‌ها
- [ ] مقایسه محصولات بر اساس کامنت‌ها
- [ ] تحلیل ترند احساسات در طول زمان
- [ ] API برای ادغام با سیستم‌های دیگر

---

**نگارش**: نوامبر 2025  
**نسخه**: 1.0.0  
**تکنولوژی**: .NET 9 + Angular 18 + Ollama + Llama 3.1  
**مجوز**: MIT
